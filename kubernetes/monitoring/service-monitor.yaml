apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: aggregator-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: worker          # Finds the service with label 'app: aggregator for metrics scrapping'
  endpoints:
  - port: metrics
    path: /metrics
    interval: 15s

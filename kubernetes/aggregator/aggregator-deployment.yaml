apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregator-deployment
  labels:
    app: aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aggregator
  template:
    metadata:
      labels:
        app: aggregator
    spec:
      containers:
        - name: aggregator
          image: my-app-service:latest
          imagePullPolicy: Never
          command: ["node", "dist/api.js"]
          envFrom:
            - configMapRef:
                name: app-config
          ports:
            - containerPort: 4002
